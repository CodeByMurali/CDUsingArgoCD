---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketrisk-deployment
spec:
  template:
    spec:
      containers:
      - name: marketrisk
        env:
        - name: ENV
          value: "dev"
        - name: GPU_ENABLED
          value: "1"
      