apiVersion: v1
kind: Service
metadata:
  name: helmapp-service
  labels:
    app: helmapp
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 9898
    protocol: TCP
    name: http
  selector:
    app: helmapp
