# Environment-specific overrides for podinfo
replicaCount: 2

image:
  tag: 6.5.4

service:
  type: ClusterIP

resources:
  requests:
    cpu: 100m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi
